The plan is to build a web based one-to-one chat interface with messages stored and encrypted on a server. 

git remote add newrepo https://github.com/askel-dev/the-eye.git
git push newrepo main

git remote set-url origin https://github.com/askel-dev/the-eye.git
git remote remove newrepo

git add index.html java.js plan.md style.css
git commit -m "Första databas versionen"
